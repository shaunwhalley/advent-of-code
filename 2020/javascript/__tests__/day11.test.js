const fs = require('fs');
const { partOne, partTwo } = require('../day11');

const input = fs.readFileSync('inputs/day11.txt', 'utf8');
const testInput = fs.readFileSync('__tests__/data/day11.txt', 'utf8');

it.only('part one example passes', () => {
  expect(partOne(testInput)).toBe();
});

it('part two example passes', () => {

});

it('part one passes', () => {

});

it('part two passes', () => {

});
